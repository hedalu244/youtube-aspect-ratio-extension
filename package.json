{
  "name": "youtube-aspect-ratio-extension",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build": "esbuild src/popup.ts src/content.ts --bundle --outdir=. --format=iife --platform=browser --target=es2020",
    "build:watch": "esbuild src/popup.ts src/content.ts --bundle --outdir=. --format=iife --platform=browser --target=es2020 --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}